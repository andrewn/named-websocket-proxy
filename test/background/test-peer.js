var chai = require('chai');

var assert = chai.assert;

describe('Peer', function () {
  describe('creation', function () {
    it('should wrap a web socket object');
    it('should give back a websocket as its value');
    it('should allow messages to be sent');
    it('should allow messages to be received');
  });
});